<h1>Expenses</h1>

<div class="row">
  <div class="large-6 columns">
    <h3 class="subheader"> All Search Records </h3>
    <%= content_tag :div, "", id: "expenses_chart", data: { expenses: @expenses } %> 
  </div>
  <div class="large-6 columns">
    <h3 class="subheader"> All records greater than £100</h3>
    <%= content_tag :div, "", id: "expenses_chart1", data: { expenses: expenses_chart1_data } %>
  </div>

</div>
<%= will_filter_tag(@expenses)  %>

<%= link_to 'New Expense', new_expense_path, class: "button", id: "new_expense_button" %>

<table>
  <tr>
    <th width="200">Name</th>
    <th width="200">Description</th>
    <th width="200">Amount</th>
    <th width="200">VAT</th>
    <th width="200">User</th>
    <th width="200">Created at</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @expenses.each do |expense| %>
  <tr>
    <td><%= expense.name %></td>
    <td><%= expense.description %></td>
    <td><%= number_to_currency(expense.amount, unit: "&pound;") %></td>
    <td><%= expense.vat %></td>
    <td><%= expense.user %></td>
    <td><%= l expense.created_at, format: :short %></td>
    <td><%= link_to 'Show', expense %></td>
    <td><%= link_to 'Edit', edit_expense_path(expense) %></td>
    <td><%= link_to 'Destroy', expense, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />


